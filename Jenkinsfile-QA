pipeline{
  agent any
  stages{
       stage('Debug') {
            steps {
                sh 'hostname'
                sh 'whoami'
                sh 'ls -l /usr/bin/docker || echo "Docker binary not here"'
                sh 'ls -la'
            }
       }
       stage('trivy test'){
            steps{
              sh'docker run --rm --user 10042:10042 -v $(pwd):/src bomanai/trivy:latest config --format json --output /src/boman_trivy_container_scan.json /./'
          }
      }
    
      // stage('Bomanai'){
      //   steps{
      //      sh '''
      //      pip install "setuptools<70.0.0"
      //      pip install --no-cache-dir --upgrade boman-cli
      //      pwd
           
      //     '''
      //   }
      // }
  }
}
